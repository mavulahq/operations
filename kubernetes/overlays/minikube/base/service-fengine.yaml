apiVersion: v1
kind: Service
metadata:
  name: fengine
  namespace: getfluxo
spec:
  selector:
    app: fengine
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
    - name: metrics
      protocol: TCP
      port: 9090
      targetPort: 3000
  type: ClusterIP
